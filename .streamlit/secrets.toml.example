# Local: copy to `.streamlit/secrets.toml` and edit.
# Streamlit Cloud: paste these key/value pairs into Settings -> Secrets.

class_password = "replace-with-class-wide-password"
admin_password = "replace-with-instructor-admin-password"
